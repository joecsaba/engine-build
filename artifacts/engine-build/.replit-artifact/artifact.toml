kind = "web"
previewPath = "/"
title = "Engine-Build"
version = "1.0.0"
id = "artifacts/engine-build"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 8080

[services.development]
run = "echo 'Engine-Build is served by the API Server on port 8080'"

[services.production]
build = [ "pnpm", "--filter", "@workspace/engine-build", "run", "build" ]
publicDir = "artifacts/engine-build/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "8099"
BASE_PATH = "/"
